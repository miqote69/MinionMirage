# Runtime state reporting test specification

| ID | Path | Action and oracle |
| --- | --- | --- |
| MNP-STATE-001 | Build | `dotnet restore --locked-mode`, then `dotnet build -c Release --no-restore`; both exit 0 with no errors. |
| MNP-STATE-002 | Load/heartbeat selected-option tuple contract | Load the real Dev Plugin. `runtime-state.json` parses as schema 2 and `mappings[]` reports exactly one selected option in fixed source order for each of thirteen sources. The fixed tuples for the other twelve sources are `(331, BattleNpc, 13910, 0, 1.0f)`, `(232, BattleNpc, 6479, 1689, null)`, `(218, EventNpc, 1017687, 0, 0.97f)`, `(398, BattleNpc, 17830, 4436, 0.84f)`, `(534, EventNpc, 1046564, 0, null)`, `(325, EventNpc, 1025589, 2382, null)`, `(298, EventNpc, 1044638, 0, null)`, `(394, EventNpc, 1033925, 2810, null)`, `(395, BattleNpc, 11366, 2810, null)`, `(98, EventNpc, 1006573, 0, null)`, `(332, EventNpc, 1033894, 0, 0.86f)`, and `(451, BattleNpc, 14545, 3645, null)`. Source `260` reports exactly one selected option: A `(260, EventNpc, 1012445, 0, 0.7f)` or B `(260, EventNpc, 1058181, 0, 0.7f)`, never both; Configuration v1 migration selects B. Both Row `260` options report `TargetModelScale=0.7f`; for any `null` scale, schema-2 JSON omits `targetModelScale`. No source tuple may be missing, extra, reordered, or changed, and `observedAtUtc` advances within 5 seconds. |
| MNP-STATE-003 | No Companion | With no minion, `selectionState=no_companion` and `companions=[]`. |
| MNP-STATE-004 | Wrong Companion | Summon a Companion other than 331, 232, 218, 398, 534, 325, 298, 394, 395, 98, 260, 332, or 451. Its `baseId` appears and `selectionState=source_not_present`; it is not tracked. |
| MNP-STATE-005 | Target ownership | Summon Companion 331, 232, 218, 398, 534, 325, 298, 394, 395, 98, 260, 332, or 451. Exactly one entry reports `isOwnedByLocalPlayer=true`, an ownership reason, and `selectionState=target_ready`. |
| MNP-STATE-006 | Screenshot-identified Y'shtola redraw | Reject Glamourer `ui_config.json` `SelectedNpc` as current-Actor evidence. From the User-provided screenshot, fix the target as Battle NPC ID 13910 / Name ID 10570 and resolve Human BNpcBase 13910 through BNpcCustomize 526 and NpcEquip 2269. Summon Companion 331. The plugin writes that customize/equipment backing once, requests the normal full redraw without a global Create hook, applies `ModelScale=1.0f`, and reports `tracked.stage=Redrawn`, `targetKind=BattleNpc`, `targetRowId=13910`, and the requested/applied model scale. Human Y'shtola appearance and clothing require User confirmation; weapon replacement remains excluded. |
| MNP-STATE-007 | Despawn/recovery | Dismiss the target. Tracking clears and selection returns to a no-target state without modifying another Companion. |
| MNP-STATE-008 | Unload | Unload with target present. The final snapshot reports `pluginState=disposed`; restoration remains separately confirmed from log/runtime observation. |
| MNP-STATE-009 | Atomic reader | Repeated background JSON reads during state changes never observe partial or malformed JSON. |
| MNP-STATE-010 | Monster redraw | Summon Companion 232. ModelChara `1689` backing is written and the normal full redraw is requested without Human-specific judgment or finalization; `tracked.stage=Redrawn`. Monster and visual confirmation remain separate observations. |
| MNP-STATE-011 | Monster lifecycle | Dismiss and resummon Companion 232. Original minion restoration/despawn remains normal and the BOSS appearance is reapplied once. |
| MNP-STATE-012 | Clothed Alisaie NPC redraw | Summon Companion 218. ENpcBase 1017687 resolves the Anamnesis-matching NPC appearance with Body `74650` (`0x1239A`, `e9114`), Hands `75439` (`0x126AF`), Legs `74650` (`0x1239A`), and Feet `74650` (`0x1239A`). The plugin writes ModelChara/customize/equipment backing once, calls the normal `DisableDraw` / `EnableDraw` full redraw, then writes `0.97f` to the recreated `CharacterBase.ModelScale` (`CharacterBase+0x2A4`) to match the User-confirmed current player multiplier. No global Create hook, immediate Human/RaceSexId/equipment judgment, post-create equipment API, or failure-triggered rollback is permitted. Completion reports `tracked.stage=Redrawn`, `targetKind=EventNpc`, `targetRowId=1017687`, and the requested/applied model scale; clothing, Human appearance, and relative height require User confirmation. Weapon replacement is excluded. |
| MNP-STATE-013 | Alisaie NPC lifecycle | Capture the original Companion draw-model scale, summon/convert Companion 218, then dismiss and resummon it or unload the plugin. The target receives `ModelScale=0.97f`; normal restoration receives the captured original scale, and the clothed Alisaie NPC appearance plus `0.97f` scale is reapplied once after resummon. |
| MNP-STATE-014 | Mapped Companion switch stabilization | With mapped Companion 218 displayed as Alisaie, summon Companion 232. If the same Actor slot reports BaseId 232 while its backing ModelChara is not the Companion-sheet source model `1782`, the plugin reports `source_model_pending` and performs no Scathach redraw. It applies BNpcBase 6479 only after backing ModelChara `1782` is present. The reverse switch waits for Companion 218 source ModelChara `1617` before applying ENpcBase 1017687. No target NPC may flash during either transient backing state; final visual confirmation is User acceptance. |
| MNP-STATE-015 | Screenshot-identified Gaia redraw | From the User-provided screenshot, fix the target as BattleNpc `17830` / ModelChara `4436`, category DemiHuman, Type `2`, Model `1041`, Base `1`, Variant `1`, with requested `TargetModelScale=0.84f`. Summon Companion `398`. The plugin waits for the Companion-sheet source ModelChara, resolves and writes ModelChara `4436` plus BNpcBase `17830`'s `BNpcCustomize` and `NpcEquip` creation inputs, requests the normal full redraw through the dedicated DemiHuman branch without Human post-create equipment-slot processing, writes `0.84f` to the recreated `CharacterBase.ModelScale`, and reports `tracked.stage=Redrawn`, `targetKind=BattleNpc`, `targetRowId=17830`, `targetModelCharaRowId=4436`, `targetModelScale=0.84`, and `modelType=DemiHuman`. The Gaia NPC appearance and relative scale require User confirmation; ModelChara and the requested scale alone are not the visual oracle. |
| MNP-STATE-016 | Gaia lifecycle | Dismiss and resummon Companion `398`. Original minion restoration/despawn remains normal and BNpcBase `17830` / ModelChara `4436` with scale `0.84f` is reapplied once after the source model becomes ready. |
| MNP-STATE-017 | DemiHuman/Monster separation regression | Resolve Gaia BNpcBase `17830` and Scathach BNpcBase `6479` through the production resolver. Type `2` Gaia fails closed if either `BNpcCustomize` or `NpcEquip` is unavailable and otherwise carries both arrays into redraw. Type `3` Scathach remains model-only and does not acquire DemiHuman customize/equipment handling. Runtime visual acceptance for Gaia and Scathach remains separate User observation. |
| MNP-STATE-018 | Screenshot-identified Pelupelu Young EventNpc redraw | From the User-provided screenshot, fix the target as EventNpc `1046564` / Name ID `1046564` (`物静かなペルペル族`). With current runtime confirming Companion `534` / source ModelChara `4391` as `マメット・ペルペル`, summon it. The production EventNpc resolver reads Human BodyType `4`, builds NPC-specific customize and equipment creation inputs from ENpcBase `1046564` or its referenced `NpcEquip`, performs the normal full redraw without the player's post-create equipment-slot API, and reports `tracked.stage=Redrawn`, `targetKind=EventNpc`, and `targetRowId=1046564`. Visual identity and clothing require User confirmation. |
| MNP-STATE-019 | Pelupelu lifecycle | Dismiss and resummon Companion `534`. Original minion restoration/despawn remains normal, the plugin waits for source ModelChara `4391`, and ENpcBase `1046564` is reapplied once without a stale previous target flash. |
| MNP-STATE-020 | Screenshot-identified Fran EventNpc DemiHuman redraw | From the User-provided screenshot, fix the target as EventNpc `1025589` / ModelChara `2382`, category DemiHuman, Type `2`, Model `1034`, Base `1`, Variant `1`. With current runtime confirming Companion `325` / source ModelChara `2412` as `マメット・フラン`, summon it. The EventNpc Type `2` resolver writes ModelChara `2382` plus ENpcBase `1025589`'s customize and equipment creation inputs, performs the normal full redraw without Human post-create equipment-slot handling, and reports `tracked.stage=Redrawn`, `targetKind=EventNpc`, `targetRowId=1025589`, `targetModelCharaRowId=2382`, and `modelType=DemiHuman`. Fran's visual identity and clothing require User confirmation. |
| MNP-STATE-021 | Fran lifecycle | Dismiss and resummon Companion `325`. Original minion restoration/despawn remains normal, the plugin waits for source ModelChara `2412`, and ENpcBase `1025589` / ModelChara `2382` is reapplied once without a stale previous target flash. |
| MNP-STATE-022 | Screenshot-identified Shiro EventNpc Human redraw | From the User-provided screenshot, fix the target as EventNpc `1044638` / Name ID `1044638` (`シロ・アリアポー`). With current runtime confirming Companion `298` / source ModelChara `2246` as `マメット・シロ`, summon it. The production EventNpc resolver reads Human BodyType `1`, builds NPC-specific customize and equipment creation inputs from ENpcBase `1044638` or its referenced `NpcEquip`, performs the normal full redraw, and reports `tracked.stage=Redrawn`, `targetKind=EventNpc`, and `targetRowId=1044638`. Visual identity and clothing require User confirmation. |
| MNP-STATE-023 | Shiro lifecycle | Dismiss and resummon Companion `298`. Original minion restoration/despawn remains normal, the plugin waits for source ModelChara `2246`, and ENpcBase `1044638` is reapplied once without a stale previous target flash. |
| MNP-STATE-024 | Screenshot-identified 2B EventNpc Human redraw | From the User-provided screenshot, fix the target as EventNpc `1033925` / ModelChara `2810`, category Human, Type `1`, Model `201`, Base `1`, Variant `1`, Hyur Midlander female adult BodyType `1`. With current runtime confirming Companion `394` / source ModelChara `2991` as `オートマトン２Ｂ`, summon it. The production EventNpc resolver builds the NPC-specific customize and equipment creation inputs from ENpcBase `1033925` or its referenced `NpcEquip`, performs the normal full redraw, and reports `tracked.stage=Redrawn`, `targetKind=EventNpc`, `targetRowId=1033925`, `targetModelCharaRowId=2810`, and `modelType=Human`. Visual identity and clothing require User confirmation. |
| MNP-STATE-025 | 2B lifecycle | Dismiss and resummon Companion `394`. Original minion restoration/despawn remains normal, the plugin waits for source ModelChara `2991`, and ENpcBase `1033925` / ModelChara `2810` is reapplied once without a stale previous target flash. |
| MNP-STATE-026 | Screenshot-identified 2P BattleNpc Human redraw | From the User-provided screenshot, fix the target as BattleNpc `11366` / ModelChara `2810`, category Human, Type `1`, Model `201`, Base `1`, Variant `1`, Hyur Midlander female adult BodyType `1`. With current runtime confirming Companion `395` / source ModelChara `2992` as `オートマトン２Ｐ`, summon it. The production Human BattleNpc resolver writes BNpcBase `11366`'s `BNpcCustomize` and `NpcEquip` creation inputs, performs the normal full redraw, and reports `tracked.stage=Redrawn`, `targetKind=BattleNpc`, `targetRowId=11366`, `targetModelCharaRowId=2810`, and `modelType=Human`. Visual identity and clothing require User confirmation. |
| MNP-STATE-027 | 2P lifecycle | Dismiss and resummon Companion `395`. Original minion restoration/despawn remains normal, the plugin waits for source ModelChara `2992`, and BNpcBase `11366` / ModelChara `2810` is reapplied once without a stale previous target flash. |
| MNP-STATE-028 | Screenshot-identified Minfilia EventNpc Human redraw | From the User-provided screenshot, fix the target as EventNpc `1006573` / Name ID `1006573` (`ミンフィリア`). With current runtime confirming Companion `98` / source ModelChara `797` as `マメット・ミンフィリア`, summon it. The production EventNpc resolver reads Human BodyType `1`, builds NPC-specific customize and equipment creation inputs from ENpcBase `1006573` or its referenced `NpcEquip`, performs the normal full redraw, and reports `tracked.stage=Redrawn`, `targetKind=EventNpc`, and `targetRowId=1006573`. Visual identity and the Scion Adventurer's Jacket appearance require User confirmation. |
| MNP-STATE-029 | Minfilia lifecycle | Dismiss and resummon Companion `98`. Original minion restoration/despawn remains normal, the plugin waits for source ModelChara `797`, and ENpcBase `1006573` is reapplied once without a stale previous target flash. |
| MNP-STATE-030 | Screenshot-identified Kuro option-A Young EventNpc redraw | With Row `260` option A selected, from the User-provided screenshot fix the target as EventNpc `1012445` (`クロ・アリアポー`), category Human, Young NPC, ModelChara `0`, Body Type `4`; retain the User-supplied official native height reference `112.3 cm` for visual comparison and apply the requested trial `CharacterBase.ModelScale=0.7f`. With current runtime confirming Companion `260` / source ModelChara `1897` as `マメット・クロ`, summon it. The production EventNpc resolver builds NPC-specific customize and equipment creation inputs from ENpcBase `1012445` or its referenced `NpcEquip`, performs the normal full redraw without the player's post-create equipment-slot API, writes `0.7f` to the recreated `CharacterBase.ModelScale`, and reports `tracked.stage=Redrawn`, `targetKind=EventNpc`, `targetRowId=1012445`, and requested/applied model scale `0.7`. Visual identity, clothing, and 70%-scale height require User confirmation. |
| MNP-STATE-031 | Kuro option-A lifecycle | With Row `260` option A selected, dismiss and resummon Companion `260`. Original minion restoration/despawn remains normal, the plugin waits for source ModelChara `1897`, and ENpcBase `1012445` plus `CharacterBase.ModelScale=0.7f` is reapplied once without a stale previous target flash. |
| MNP-STATE-032 | Screenshot-identified Ryne EventNpc Human redraw | From the User-provided screenshot, fix the target as EventNpc `1033894` / NPC Name ID `1033894` (`リーン`), category Human, mapping `TargetModelCharaRowId=0`, and requested mapping contract `TargetModelScale=0.86f`. With current runtime confirming Companion `332` / source ModelChara `2486` as `マメット・リーン`, summon it. The plugin waits until backing source ModelChara `2486` is present, then the production EventNpc resolver builds NPC-specific customize and equipment creation inputs from ENpcBase `1033894` or its referenced `NpcEquip`, performs the normal full redraw without the player's post-create equipment-slot API, and writes the requested `0.86f` to the recreated `CharacterBase.ModelScale`. Completion reports `tracked.stage=Redrawn`, `targetKind=EventNpc`, `targetRowId=1033894`, `targetModelCharaRowId=0`, `modelType=Human`, and the requested model-scale write. `mappings[].TargetModelScale=0.86f` is the contract oracle; the existing `IPluginLog` draw-model scale-write log is write-request evidence, not a readback. The post-redraw actual `CharacterBase.ModelScale` predicate is `PASS` or `FAIL` only when an existing passive observation obtains that readback; if it is unavailable, record `UNEXECUTED`. The mapping value, `Redrawn`, and the scale-write log do not substitute for actual-scale readback. Visual identity, clothing, and relative height remain User acceptance. This case requires no new diagnostic path or product code. |
| MNP-STATE-033 | Ryne lifecycle | Dismiss and resummon Companion `332`. Original minion restoration/despawn remains normal; the plugin waits for source ModelChara `2486`, then reapplies ENpcBase `1033894` and the requested `CharacterBase.ModelScale=0.86f` once without a stale previous target flash. This lifecycle case does not infer a post-redraw actual model scale or visual verdict from mapping `TargetModelScale`; each remains a separate runtime/User observation. |
| MNP-STATE-034 | Screenshot-identified Azeyma BattleNpc Monster redraw | From the User-provided screenshot, fix the target as BattleNpc `14545` / ModelChara `3645` (`アーゼマ`), Monster Type `3` / Model `776` / Base `1` / Variant `2`, Data `ModelOnly`, with `TargetModelScale=null`. With current runtime confirming Companion `451` / source ModelChara `3562` as `マメット・アーゼマ`, summon it. The plugin waits until backing source ModelChara `3562` is present, writes ModelChara `3645`, and requests the normal full redraw through the Type `3` Monster model-only path. It must not add `BNpcCustomize`, `NpcEquip`, Human, or DemiHuman processing, and it performs no `CharacterBase.ModelScale` write. Completion reports `tracked.stage=Redrawn`, `targetKind=BattleNpc`, `targetRowId=14545`, `targetModelCharaRowId=3645`, and `modelType=Monster`. The Azeyma visual identity and model scale remain separate User acceptance. |
| MNP-STATE-035 | Azeyma lifecycle | Dismiss and resummon Companion `451`. Original minion restoration/despawn remains normal; the plugin waits for source ModelChara `3562`, then reapplies BNpcBase `14545` / ModelChara `3645` once through the Type `3` Monster model-only path without a stale previous target flash, `BNpcCustomize`, `NpcEquip`, Human/DemiHuman processing, or a `CharacterBase.ModelScale` write. Visual acceptance remains separate User observation. |
| MNP-STATE-036 | Row260 Configuration v1-to-v2 migration/default B | Start with a valid Configuration v1 that has Row `260` enabled and no target option. Load the real Dev Plugin. It migrates and persists Configuration v2 with Row `260` selected to option B, EventNpc `1058181`; `runtime-state.json` reports exactly one Row `260` B tuple `(260, EventNpc, 1058181, 0, 0.7f)`. Repeat with Row `260` disabled in Configuration v1: Configuration v2 retains it disabled and persists option B (`1058181`), while `runtime-state.json` still reports exactly the one B tuple. No redraw or `CharacterBase.ModelScale` write occurs until Row `260` is re-enabled; after re-enable, the normal EventNpc option-B path waits for source ModelChara `1897`, then redraws and requests `0.7f`. The other twelve source tuples and their scales remain unchanged. |
| MNP-STATE-037 | Screenshot-identified Row260 option-B EventNpc redraw | With Configuration v2 Row `260` option B selected, fix the User-screenshot target as EventNpc `1058181`, Human Miqo'te female / BodyType `4` / ModelChara `0`, provisional `TargetName=EventNpc 1058181`, and requested `TargetModelScale=0.7f`. With current runtime confirming Companion `260` / source ModelChara `1897` as `マメット・クロ`, summon it. The plugin waits for source ModelChara `1897`, then uses the normal EventNpc resolver to build the NPC-specific customize/equipment inputs, requests the normal full redraw, and writes the requested `0.7f` to the recreated `CharacterBase.ModelScale`. It reports `tracked.stage=Redrawn`, `targetKind=EventNpc`, `targetRowId=1058181`, `targetModelCharaRowId=0`, and selected target B. `TargetModelScale=0.7f` and its write log are contract/write-request evidence only; an actual post-redraw scale is `PASS` or `FAIL` only when an existing passive readback obtains it, otherwise `UNEXECUTED`. Visual identity, clothing, and relative height remain User acceptance. No new resolver, rollback, or scale branch is permitted. |
| MNP-STATE-038 | Row260 summoned option A/B switch and failure-state accuracy | With Companion `260` transformed under option A, change its selected target A→B, then B→A while it remains summoned. On a Framework update, the plugin first restores the original appearance and captured pre-conversion scale, then applies only the selected target through the normal EventNpc resolver after source ModelChara `1897` is ready. No stale old target backing or old appearance/scale may remain; `runtime-state.json` reports one selected Row `260` target and `tracked.stage=Redrawn` after each switch. For negative coverage, reproduce a normal option-B redraw failure and then select A: `mappings[]` changes to A, while `failedActor` remains the actual option-B failure-time tuple and the same failed actor receives no additional write, including after disable/re-enable. Separately reproduce an A→B restoration failure: `mappings[]` changes to B, while `failedActor` remains the actual option-A failure-time tuple and the same failed actor again receives no additional write. In each failure branch, keep the same Companion `ActorIdentity` present while local-player observation and then ownership observation are temporarily unavailable; `failedActor` and `lastError` remain unchanged, and observation recovery causes no additional actor write. The latch clears only when the failed object slot disappears or its identity changes, or when a different/reacquired `ActorIdentity` is selected; ownership or local-player availability alone is not an exit oracle. Both options request `0.7f`; the requested write does not establish an actual post-redraw scale or visual verdict. No B-specific resolver, scale, rollback, or direct-write branch is permitted. |
| MNP-STATE-039 | Row260 selection persistence/reload | Select option B, reload the actual Dev Plugin, and verify the persisted Configuration v2 and `runtime-state.json` retain B without changing enabled state or another source. Repeat after selecting option A. Each reload reports exactly the selected Row `260` tuple, preserves the thirteen-source count, and uses the normal selected-target path only after source ModelChara `1897` is ready. |
| MNP-STATE-040 | Row260 invalid persisted target fail-safe fallback | Start with Configuration v2 whose persisted Row `260` target is neither option A nor B. Load the real Dev Plugin. It fails safe by normalizing and persisting the defined default option B (`1058181`), reports exactly the B tuple, does not select an arbitrary target or create a new resolver path, and leaves the other twelve source mappings and their scales unchanged. |
| MNP-STATE-041 | Row260 option-B static candidate-data call trace | Inspect the production source call trace from Row `260` option-B candidate data to the selected mapping. It must enter the existing EventNpc resolver and existing restore → source-model-ready → reapply path, with no option-B-specific resolver, scale, rollback, or direct-write branch. This static oracle proves only path sharing; redraw, requested scale write, actual-scale readback, and visual acceptance remain governed by the runtime/User cases. |
| MNP-STATE-042 | Standalone target selection switch | Change a multi-option Companion such as Row `260` between option A and B. Minion Mirage must not inspect or invoke Penumbra, Glamourer, or any other plugin API. On the Framework thread it changes the tracked mapping to `Pending` and uses only the existing backing write → redraw → finalize → verify path. The applied log and runtime state identify the selected EventNpc, and the User confirms the visible NPC matches the selected option. |
| MNP-UI-001 | Default and persistence | With no prior config, all thirteen source mappings are enabled and Row `260` defaults to option B. Disable one mapping in the actual configuration window, close/reopen the window or reload the plugin, and verify only that mapping remains disabled while the other twelve remain enabled and Row `260`'s stored target selection remains unchanged. No extra mappings or settings are created. |
| MNP-UI-002 | Roster content and game order | Open the actual configuration window. Every configured source mapping appears once, ordered by the current game-data `Companion.Order` value ascending; equal or unavailable order values use `SourceCompanionRowId` ascending as the deterministic fallback. Search and group filtering preserve that relative order. Each item shows the source Companion's real game icon, localized Companion-sheet name, selected target name, appearance category, and one visible enabled/disabled control. Missing icon data degrades to the text and control without crashing. |
| MNP-UI-003 | Settings-hosted language selection | Verify the language selector is not permanently visible. Open the localized `Settings` menu, then select Automatic, English, Japanese, German, and French in turn from its UI-language submenu. UI labels and language names change immediately and persist. Automatic follows `IClientState.ClientLanguage`; Companion names use that same current game-data language behavior as Minion Scaler. |
| MNP-UI-004 | Disable active mapping | With Companion 331 transformed, switch its mapping OFF. On the next Framework update the existing tracked appearance is restored through the normal restoration path and no further target backing is applied while disabled. Re-enable it and verify the normal source-model-ready path reapplies BNpcBase 13910 once. |
| MNP-UI-005 | Mapping isolation | Toggle each row independently. Changing one source row does not alter the enabled state, target selection, appearance, or model scale of any other mapping. |
| MNP-UI-006 | Product identity and window lifecycle | Build the renamed project and verify the artifact is `MinionMirage.dll`, its manifest and Dalamud plugin entry display `Minion Mirage`, and the configuration window title is `Minion Mirage`. Migrate the current local Configuration v2 type identity from `MinionToNPC.Configuration, MinionToNPC` to `MinionMirage.Configuration, MinionMirage` without changing `UiLanguage`, `DisabledCompanionRowIds`, or `SelectedTargetRowIds`, then point the existing Dev Plugin registration to the renamed DLL. Open the window from Dalamud's plugin configuration action, close/reopen it, unload/reload the plugin, and verify there are no duplicate draw callbacks, stale windows, configuration errors, or reset selections. Visual layout review is User acceptance and remains `UNEXECUTED` until observed in game. |
| MNP-UI-007 | Bulk enable/disable | In the actual configuration window, click the localized enable-all button and verify all thirteen individual mapping controls become enabled and one persisted config update represents the resulting set. Click the localized disable-all button and verify all thirteen controls become disabled, an actively transformed mapped Companion is restored through the existing path, and no target is applied while disabled. Stored target selections, including Row `260` option A/B, remain unchanged. Re-enable all and verify the normal mapping path resumes. No extra mapping or global runtime mode is created. |
| MNP-UI-008 | Search and empty state | Search by a localized Companion name, invariant source name, and every target candidate name. Matching is case-insensitive, only matching items remain, and the visible/total count updates without changing mapping state. A query with no match shows the localized empty state and does not crash or mutate configuration. |
| MNP-UI-009 | Group filters and colors | Select All, Adult Human, Young Human, DemiHuman, and Monster in turn. Human grouping is derived from the selected target's resolved appearance (`BodyType=4` is Young Human; other Human body types are Adult Human), so changing a multi-target selection updates its group without duplicated mapping metadata. Each group uses one consistent accent color across every card/list row while retaining a visible text group label, and the filter controls wrap instead of clipping when the window narrows. Each selection displays the group count and preserves each mapping's enabled state. |
| MNP-UI-010 | Card/list views | Switch between card and list views using icon-only controls with distinct card-grid/list glyphs and localized hover tooltips. Both views expose the same source mappings, minion icons, names, selected target labels, group labels, individual controls, and any required multi-option selector; switching view does not persist or alter runtime configuration. Card columns adapt to the available content width without horizontal scrolling or clipped controls. |
| MNP-UI-011 | Mock scope and compact header | The plugin window contains a menu bar with `Settings`, the search/filter/view/bulk controls, and the roster. It does not contain the local mock's sidebar, brand/logo, `LOCAL PROTOTYPE`, Dalamud runtime-status text, large roster title, or a permanently visible language selector. The active/total and visible/total counts remain readable at the minimum supported window size. |
| MNP-UI-012 | Row260 multi-target selector | In both card and list views, Row `260` keeps exactly one ON/OFF control and, because it has exactly two candidates, exposes an explicit combo/radio selector with A `Khloe Aliapoh` and B `EventNpc 1058181`; the selected state is unambiguous. Only sources with two or more candidates expose that selector; every single-candidate row retains the prior display. Changing A/B does not toggle ON/OFF, and changing ON/OFF does not mutate the stored A/B choice. Searches for either `Khloe Aliapoh` or `EventNpc 1058181` match Row `260`. The selected target's semantic UI/configuration state updates immediately without waiting for an animation or redraw; resulting appearance remains separate User acceptance. |
| MNP-STATE-047 | All-minion native-action unlock | Enable the normal-summon experimental switch in a minion-hidden area. The installed `GetActionStatus` detour calls the original function and returns `0` for every Companion Row when all predicates hold: switch ON, `ActionType.Companion`, and original status equals the observed rejection `1325`. On a Companion click, the installed `UseAction` detour reads that Row's original status and calls the original function unchanged, followed by native `UseActionLocation` once when the original status is `1325`. If the downstream call is rejected, invoke local `SetupCompanion(clickedRow, 1)` once, or `SetupCompanion(0, 1)` when the clicked Row is already active. For any other original status it makes no additional call. It never queues a Row or retries automatically. `runtime-state.json` records both hook states, scope, status-bypass data, clicked RowId, and whether original action, downstream action, local summon/replace, local dismiss, or rejection occurred. |
| MNP-STATE-048 | Native game-icon summon and hook isolation | With both hooks enabled, confirm multiple game minion icons become active and click them normally. In prohibited content where the public ActionManager path rejects, the local Companion synchronization transition parameter `1` must produce the clicked Companion and its native transition/VFX; after ObjectTable exposes it, the existing mapping path may transform it. Turn the switch OFF and verify both hooks are disabled, hidden-area icons return to their original inactive state/status `1325`, and mapping enabled/selected state, conversion/restoration stages, and failure latch are unchanged. Game-icon state, successful summon, dismissal, replacement, and native VFX are User-visible runtime acceptance. |
| MNP-UI-014 | Normal-summon unlock presentation | The visible `Experimental` section shows only one labeled `Enable normal minion summon (experimental)` switch. Readable smaller warning text states that it enables all game minion icons and ordinary summon in normally prohibited areas and may crash the game. The removed force-display control and its warning do not appear. The state is explicit without relying on color, hover, icon, or tooltip. |
| MNP-UI-015 | Unowned Companion presentation | Open both card and list views on a character that owns at least one configured Companion and does not own at least one other configured Companion. For every unowned source, `UIState.IsCompanionUnlocked(SourceCompanionRowId)` is false, the item remains visible in normal `Companion.Order` position, its icon/text/accent are visibly subdued, and a localized `Not owned` label is readable without relying on color or hover. Its target selector and individual ON/OFF control cannot be operated. Owned sources retain the normal group accent and controls. Search, group filters, and card/list switching retain both classes and their relative order. Opening and closing the window does not add the Row to `DisabledCompanionRowIds`, alter its selected target, or persist ownership state. If `UIState` is temporarily unavailable, the UI must not falsely mark every source unowned. Actual appearance and interaction are User-visible acceptance in the Dalamud window. |
| MNP-STATE-049 | Native replacement isolation | With one locally owned Companion present in prohibited content, click a different minion icon. After ActionManager rejects, Minion Mirage passes the exact clicked Row and transition parameter `1` to local `SetupCompanion` once. The native Companion transition replaces the prior Companion; Minion Mirage does not select/filter the clicked Row and applies the configured NPC only after ObjectTable exposes it. |
| MNP-STATE-050 | Redrawn DrawObject recreation recovery | With a mapped Companion such as Row `534` verified at `Redrawn`, replace its DrawObject while ObjectIndex, GameObjectId, EntityId, and BaseId remain unchanged. On a Framework update, Minion Mirage compares the current game-actor DrawObject pointer with the pointer stored at successful verification. When they differ, it records `draw_object_recreated` and resets the same tracked actor to the existing `Pending` stage without recapturing or overwriting its original minion appearance. The normal write → disable → hidden write → enable → finalize → verify path runs once and returns to `Redrawn`; an unchanged DrawObject and unchanged configured appearance cause no write. No external state, collection, mod state, or plugin API is inspected. Runtime/log evidence must show the configured NPC reapplied and the User confirms it is visible again. |
| MNP-STATE-051 | Native same-icon dismissal | With Companion Row `534` visible in prohibited content, click Row `534` again. After ActionManager rejects, Minion Mirage calls local `SetupCompanion(0, 1)` once to run the native dismissal transition. ObjectTable remains without a locally owned Companion until the User explicitly clicks a minion icon again. There is no queued Row or automatic respawn. |
| MNP-STATE-052 | Departing-actor redraw and scale isolation | With a mapped Companion whose target has a non-null scale (for example Row `354` at `0.62f`) verified at `Redrawn`, first summon and dismiss that same Companion repeatedly, then summon it and click a different Companion. While the tracked actor identity is still present, Minion Mirage compares the current `CharacterBase.ModelScale` with the requested scale and writes only when the absolute difference is at least `0.0001f`, including any transient interval where the scan returns no unique candidate. A stable already-scaled actor receives no repeated raw scale write. It performs no appearance write, `TryApplyAppearance`, `DisableDraw`, `EnableDraw`, equipment load, or captured-original-scale restore during normal dismissal/replacement. The still-displayed old NPC must not resize to its original scale before disappearing or replacement. Once the actor identity leaves, tracking clears without a plugin redraw or automatic respawn. Only the new mapped Companion may enter the normal apply/redraw path. As controls, switching that active mapping OFF restores the captured original appearance and scale once, and unloading the plugin with it present does the same. User observation confirms the native transition/VFX and absence or presence of the hitch. |
| MNP-STATE-053 | GPose visible representation apply | With a mapped Companion already verified at `Redrawn` in the normal field, enter GPose. Preserve the normal tracked state and its captured original appearance. After GPose representations appear, resolve exactly one separate representation from the normal Companion identity and apply the same selected mapping and requested scale through the existing write → disable → hidden write → enable → finalize → verify pipeline. The GPose-visible actor reaches `Redrawn`; the User confirms the selected NPC is visible in GPose. The normal tracked Companion is not restored, recaptured, or overwritten. |
| MNP-STATE-054 | GPose unresolved representation isolation | Enter GPose when no unique representation matches the normal Companion identity, or while representations are still being created. Minion Mirage performs no appearance write to an arbitrary actor, reports/retains a waiting or unresolved GPose state, and continues passive observation. It does not clear the normal tracked state, alter mapping configuration, invoke summon actions, or inspect another plugin. If one unique representation later appears, MNP-STATE-053 proceeds once. |
| MNP-STATE-055 | GPose exit and normal-path recovery | After MNP-STATE-053 reaches `Redrawn`, exit GPose. Release only the GPose representation tracking without restoring or redrawing the destroyed GPose copy. Resume the preserved normal Companion tracking when that identity is still present; otherwise allow the existing normal scan/acquire path to resume. Mapping OFF and plugin unload still restore the normal Companion from its original pre-conversion capture. Normal summon, replacement, dismissal, native VFX, and experimental summon-hook behavior remain unchanged. |
| MNP-STATE-056 | Same-DrawObject territory appearance-drift recovery | Verify a mapped Companion at `Redrawn`, then perform an in-city teleport that restores the source minion model/customize/equipment while ObjectIndex, GameObjectId, EntityId, BaseId, and the nonzero DrawObject address remain equal to the tracked values. On the next Framework update, Minion Mirage compares the actual actor backing with the selected target through the existing appearance oracle, records `appearance_drift_detected`, and resets the same tracked actor to the existing `Pending` stage without recapturing or overwriting its original minion appearance. The normal write → disable → hidden write → enable → finalize → verify path runs once and returns to `Redrawn`; a subsequent stable update performs no appearance write or redraw. Mapping OFF and plugin unload still restore the original capture. No summon action, external plugin/API inspection, new resolver, or parallel redraw path is used. Runtime-state must move from the stale `Redrawn`/source-model mismatch through `appearance_drift_detected` back to `appearance_redrawn`, and the User confirms the selected NPC remains visible after teleport. |
| MNP-UI-016 | Wind-up Pixie EXD option name | Read Japanese `ENpcResident.Singular` for EventNpc rows `1031809`, `1031890`, and `1031806`, then open the Wind-up Pixie target selector. The labels are exactly `ウィン＝ニイ`, `アン＝ラド`, and `ティル＝ベーク`. The three RowIds, ordering, selected target, and `0.62f` scales are unchanged. |
| MNP-UI-017 | Slash-command registration and lifecycle | Load the real plugin and execute `/minionmirage` while its configuration window is closed. Exactly one registered handler opens the existing Minion Mirage configuration window without changing mappings, configuration values, or runtime conversion state. Execute it again while the window is already open; the same window closes without changing product state. Execute it once more and verify it reopens. Unload the plugin and verify the handler is removed; reload and verify successive invocations still alternate one window between open and closed without a duplicate-handler error. The packaged `MinionMirage.json` and repository `Description` contain `Replaces selected minions with NPCs.` and `Command: /minionmirage` on separate lines. |
| MNP-DIST-001 | Public-source/artifact-repository boundary | Confirm `miqote69/MinionMirage` is `PUBLIC`. Inspect the separate public distribution repository default branch and releases. It contains only distribution metadata, the installer icon, and packaged release assets; no plugin `.cs`, `.csproj`, local configuration, runtime state, or internal development documentation is copied into the distribution repository. Unexpected product-source content in the distribution repository is FAIL. |
| MNP-DIST-002 | Opaque custom repository feed | Perform an anonymous HTTP GET of the fixed `.dalamud/2d478e48ca56c3c2453cc2e69d41d02e54c41e23c6c69cf805b332a916273011/repo.json` URL. Require HTTP 200, UTF-8 JSON without BOM, a root array with exactly one Minion Mirage entry, `InternalName=MinionMirage`, `DalamudApiLevel=15`, `AssemblyVersion` equal to the packaged assembly, and nonempty `IconUrl`, `DownloadLinkInstall`, `DownloadLinkTesting`, and `DownloadLinkUpdate`. Authentication, a GitHub login page, redirect to a private raw URL, invalid JSON, or a version mismatch is FAIL. |
| MNP-DIST-003 | Release ZIP and icon identity | Build the fixed public source commit in Release, package only `MinionMirage.dll`, `MinionMirage.deps.json`, `MinionMirage.json`, and `images/icon.png`, and publish that ZIP as the matching public distribution release asset. Resolve install, testing, and update URLs anonymously through `downloads.miqote69.com`; each terminal payload must be a ZIP whose SHA-256 equals the published artifact. Resolve `IconUrl`; require the committed 512x512 PNG and matching SHA-256. Missing or extra entries, HTML/error payloads, authentication, or differing hashes are FAIL. |
| MNP-DIST-004 | Runtime isolation regression | Compare the fixed public source commit before and after distribution setup. Product runtime `.cs` behavior, mapping tuples, configuration schema, GUI behavior, and experimental summon path remain unchanged except for the explicitly released product fixes. Load the packaged DLL through the normal installed-plugin path and confirm it reaches the existing running state. The distribution service is used by Dalamud installation/update only; the loaded plugin performs no new outbound request. |
| MNP-DIST-005 | Installed Plugin Installer icon | Open the published release ZIP and require its `MinionMirage.json` to contain the same nonempty `IconUrl` as the custom-repository entry. Install or update that ZIP through the actual third-party repository path, then inspect the installed local manifest and reopen or refresh the Plugin Installer image cache. PASS requires the installed manifest to retain the same `IconUrl` and the Minion Mirage artwork to render instead of Dalamud's default icon. An outer-feed-only URL, a packaged `images/icon.png` alone, HTTP 200 alone, or Dev Plugin disk-icon rendering is FAIL for this normal-install path. |

All runtime cases use the actual plugin and FF14 object table; MNP-STATE-041 is a static inspection of the production source path. No fixture or synthetic Companion can satisfy runtime MNP-STATE-002 through MNP-STATE-048 or MNP-UI-001 through MNP-UI-014, and no fixture or stub source can satisfy MNP-STATE-041.
